# 任务计划 1：面向 Qwen3-TTS、OmniVoice 与 dots.tts 的剧本拆分与分析 Prompt

## 结论与范围

本文件是设计提案，不实施任何前端或后端改动。后续生产候选固定为用户已选定的 `Qwen3-TTS-12Hz-1.7B-Base`、`OmniVoice` 和 `dots.tts-base`；这不是把 CER（字符错误率）相加后的自动结论，最终仍须与人工试听和 Seed-TTS 测试分别比较后，才选定唯一生产模型。

当前根目录 `index.html` 的分析 Prompt 仍面向 IndexTTS2：它输出 `emotion` 与 `intensity`，并要求从情绪预设名中选择。三种候选克隆模型目前却只消费“目标文本 + 参考音频 + 参考转写”，所以不能把现有情绪标签当成已经生效的表演控制。Prompt 改造的目标应是：

1. 让拆分边界、标点和上下文更适合三种模型逐条合成；
2. 生成可审阅、可路由到参考音色档案的表演元数据；
3. 保持 `dialogue`、`bgm`、`bgImage` 三种时间轴类型及既有 `emotion`、`intensity`、`break_duration` 字段兼容；
4. 绝不把“害怕”“压低声音”等舞台标签写进 `text_content` 或 `prompt_text`，否则会被念出来或破坏准确参考转写。

## 已核对的事实

### 评测边界

`~/github/scoring-for-TTS/longAudioTestV6` 至 `V9` 的报告均明确：SenseVoice CER 与 Whisper-large-v3-turbo CER 必须独立排名，不得平均为综合分，且两者都不衡量音色、情绪、表演或自然度。例如 V9 中 Qwen3-TTS 的 SenseVoice CER 为 0.0332（第 1），dots.tts-base 为 0.1402（第 2），但 Whisper 的第一名是 LongCat；这说明这些结果不能单独推翻或确认当前三模型的产品候选范围。

### 三模型的克隆共同点与差异

| 模型 | 可靠的克隆输入 | 当前后端实际转发 | 对本计划的含义 |
| --- | --- | --- | --- |
| Qwen3-TTS Base | `ref_audio`、逐字 `ref_text`、目标 `text`、语言 | `prompt_text` 映射为 `ref_text` | 参考文本必填；每条目标台词应保持自然标点，不可掺入表演标签。 |
| OmniVoice | `ref_audio`、`ref_text`、目标 `text`、可选语言 | `prompt_text` 映射为 `ref_text` | 官方建议 3–10 秒参考片段；同语言、干净的参考是基线。 |
| dots.tts-base | `prompt_audio`、逐字 `prompt_text`、目标 `text` | 原样传递 `prompt_text` | 参考音频与准确转写构成 continuation cloning（续接式克隆）；不要用不存在的“情绪 prompt”字段。 |

Qwen3-TTS Base 的官方克隆接口只声明参考音频、参考转写和目标文本；OmniVoice 的 `instruct` 是无参考音频的 Voice Design（音色设计）模式，不应在未验证能与克隆共存前当成克隆表演参数；dots 当前公开调用也没有语义化的情绪指令字段。当前 `TTS-and-VoiceDesign` 的三个 worker 同样没有读取或传递 `emotion`、`intensity`、`instruction` 一类字段。

参考： [Qwen3-TTS 官方 README](https://github.com/QwenLM/Qwen3-TTS/blob/main/README.md)、[OmniVoice 官方 README](https://github.com/k2-fsa/OmniVoice)、[dots.tts 官方模型卡](https://huggingface.co/dots-studio/dots.tts-base)。本地接入事实见 [`docs/TTS-and-VoiceDesign接入.md`](docs/TTS-and-VoiceDesign接入.md)。

## 目标输出契约（提案）

保留现有台词的必填字段，新增一个只描述、不直接朗读的 `performance` 对象。它是可选的附加字段：旧工程没有它时按当前逻辑工作；新工程仍使用 `dialogue` 类型，不能另造 `emotion` 或 `sfx` 时间轴块。

```json
{
  "type": "dialogue",
  "role_name": "老李",
  "text_content": "别接那个电话！",
  "emotion": "害怕",
  "intensity": "较强",
  "break_duration": 0.35,
  "performance": {
    "primary_emotion": "害怕",
    "emotion_strength": 4,
    "delivery": ["压低", "急促", "轻微发颤"],
    "pace": "快",
    "loudness": "中",
    "pitch_tendency": "偏高",
    "continuity": "承接上一句对死亡事实的确认，恐惧升级",
    "reference_profile": "tense"
  }
}
```

字段约束如下：

- `emotion` 和 `intensity` 继续严格使用现有资源库可选值，供旧工程和 IndexTTS2 保持兼容。
- `emotion_strength` 只能为 1–5；`pace` 为 `慢/正常/快`；`loudness` 为 `低/中/高`；`pitch_tendency` 为 `偏低/中性/偏高`。
- `delivery` 最多选 3 个，限定为 `平稳、克制、压低、犹疑、急促、发颤、冷硬、哽咽、爆发、轻快`。它不是读给 TTS 的文本。
- `continuity` 最多 40 个汉字，只解释前后关系，供人工和后续路由审核；不能重述台词，也不进入 TTS 请求。
- `reference_profile` 是候选档案名（如 `neutral`、`tense`、`restrained`），不是已支持的模型参数。无对应档案时必须回退 `neutral`。

现有 `project-storage.js` 的 `normalizeDialogueLine` 以 `...source` 保留未知附加字段，因此该做法可在实现时保持旧工程可读；但实施前仍需写出显式校验和 P1 回归，不能仅依赖这一实现细节。

## 拆分规则

三模型当前都是逐条合成。为了避免后端再次按 60 或 120 字符硬切、插入固定 chunk 停顿，建议将“表演单元”作为拆分单位：

1. 一次说话轮次中，情绪、说话对象、语速或声量明显变化时切行；不要为了每一个逗号切行。
2. 中文台词和旁白优先为 20–55 个字符；单行上限 55 个字符。短促反应可短于 20 字，但必须与相邻动作/停顿一起判断，避免连续单字行。
3. 不合并两个角色、引号内外不同说话者或跨情绪转折；叙述可按一次自然换气合并。
4. 保留原始汉字、数字读法策略和原始标点。允许仅为自然呼吸补上原文语义等价的常规标点，禁止加括号舞台说明、情绪词或人名提示。
5. `break_duration` 只表达台词结束后的墙钟停顿；音效落点仍按现有 `sfx.position` 相对于念白时长计算，不把停顿重复算入。

55 字的约束特意低于当前 OmniVoice worker 的 60 字切块默认值。它不是模型能力上限，而是为了让每个可编辑台词行在三模型下都有一致的上下文和停顿控制边界。

## 建议替换的“脚本分析” Prompt 骨架

以下内容应替换目前“直接用于 IndexTTS”的表述；`${...}` 仍由现有运行时注入。音效、BGM、滤镜、背景图现有章节保留，但需把下列章节放在它们之前，并删除“生成结果将直接用于 IndexTTS”的单模型前提。

```text
你是有声剧的剧本编辑与配音导演。把小说原文转换成可由
Qwen3-TTS Base、OmniVoice 或 dots.tts-base 逐条克隆合成的严格 JSON 时间轴。

最高优先级：逐字保留原文内容。text_content 只能出现最终应被朗读的正文和正常标点；
不得写入情绪标签、舞台说明、SSML、角色提示、括号说明或模型指令。

每个 dialogue 都必须输出：type、role_name、text_content、emotion、intensity、
break_duration、performance。

拆分规则：以单次自然表演和换气为单位；一般 20–55 个字符，上限 55 个字符；
情绪、对象、语速、声量或叙事视角明显变化时切分；不要因每个逗号机械切分。

performance 是元数据，绝不写进 text_content：
- primary_emotion 必须等于 emotion；emotion_strength 为 1–5；
- delivery 从 [平稳、克制、压低、犹疑、急促、发颤、冷硬、哽咽、爆发、轻快] 中选 0–3 个；
- pace 为 慢/正常/快，loudness 为 低/中/高，pitch_tendency 为 偏低/中性/偏高；
- continuity 用不超过 40 个汉字说明承接的已知上下文；
- reference_profile 只能为 neutral、restrained、tense、emphatic 之一；不确定时 neutral。

emotion 必须取自：${emotionList}；intensity 必须为：微弱、稍弱、中等、较强、强烈。
旁白不能因为“旁白”身份被强行标为平静，应按文本事实给出克制的表演元数据。

${sfxSection}
${bgmSection}
${filterSection}

只输出严格 JSON 数组，不要 Markdown 或解释。
小说原文：
<novel_content>${rawScript}</novel_content>
```

实现时，现有 BGM、SFX、`bgImage` 的格式说明和资源名硬约束必须原样保留在 Prompt 中。尤其不能让 LLM 生成独立 `sfx` 块，也不能让它为尚未生成的 AI 音效编造资源库名称。

## 从分析元数据到实际表现的正确链路

单纯增加 Prompt 元数据不会让三种克隆模型自动改变情绪。可验证的第一条表演控制路径是“参考音色档案路由”：同一位已授权说话人，录制并逐字转写 `neutral`、`restrained`、`tense`、`emphatic` 等短参考片段；LLM 只建议档案，用户可以改选；合成时将选中的音频与其真实转写成对传给模型。

推荐先只准备 `neutral` 和 `tense` 两档，以每位角色、每个模型独立做 A/B 试听。若某模型没有稳定收益，仍使用同一稳定的 `neutral` 参考，不以伪“情绪参数”强行覆盖。参考音频须获得授权、单人、干净、与 `prompt_text` 逐字一致；OmniVoice 的官方建议为 3–10 秒，项目统一的 5–10 秒工程范围可继续作为首轮基线。

## 实施顺序与验收

1. 先固化一份不含任何新增功能的基线工程和三模型生成设置（模型 revision、参考音频、参考转写、语言、采样参数、随机种子）。
2. 只启用新分析 Prompt，检查每行字符数、原文拼接、JSON、`emotion/intensity` 枚举和 `performance` 枚举；不改变 TTS 请求，确认它不会悄悄影响文本保真。
3. 再实现“参考音色档案”选择与回退，逐模型对 `neutral`/`tense` 做盲听 A/B；保留每条结果的档案 ID 和参考文本哈希。
4. 对通过人工预筛的设置，用相同 `ai_deal` 文本分别跑 CER；SenseVoice 与 Whisper 的原始值独立记录，不求平均。
5. 使用 scoring-for-TTS 中适用的 Seed-TTS 流程和独立人工盲听表评估相似度、自然度、情绪贴合、角色一致性、异常率与生成时延。V6–V9 的受限长音频规则不含 Seed-TTS，不能把 Seed 结果冒充为这些报告的一部分。
6. 只有预先写明淘汰规则、完成复测和人工复核后，才可从三者选出唯一生产模型。

实现时必须保持 `UnitaleDB`、旧 `localStorage` 键、工程 schema 与 `dialogue`/`bgm`/`bgImage` 时间轴语义；至少运行内联脚本语法检查和 `node scripts/validate-p1-regression-fixture.mjs`（该脚本在当前工作区缺失时，先恢复或确认其权威位置，不能跳过验证）。
