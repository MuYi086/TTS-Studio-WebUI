我期望index.html中<!-- 配音/生成/播放 -->下方<div class="bg-white p-3 rounded-xl border border-slate-200 shadow-sm space-y-2">增加一个select,选择框俩项内容，分别是"关闭可控克隆"和"开启可控克隆"。默认选择是"关闭可控克隆"，当选择是关闭时，点击<button data-testid="generate-all-lines" @click="generateAllLines"> 执行的时候大模型永远当成是极致克隆返回生成的剧本.也就是返回的每句台词都标记"clone_mode": "ultimate", 只有选择是"开启可控克隆时",大模型在返回剧本时才按照执行原来的按照台词判断时可控克隆还是极致克隆返回ultimate或者controllable