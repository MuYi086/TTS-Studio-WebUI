# 待完成功能

## 核验结论

`webUI/` 尚未完成 `重构顺序计划.md` 定义的 P2 重构，故保留全部
`P0-*.md` 文档。本次结论基于静态实现核对与构建验证；未把“代码存在”
等同于“完整回归已通过”。

已通过的基础检查：

- `node scripts/validate-p1-regression-fixture.mjs`
- 在 `webUI/` 下执行 `npm run build`

## 必须补齐的功能

1. 完成音色、`SFX`（音效）和 `BGM`（背景音乐）的资源试听与波形裁剪。
   - `P0-功能基线清单.md` 要求音色参考音频和 `SFX/BGM` 可试听，并保留
     波形裁剪与进度可视化。
   - [SfxPanel.vue](src/components/SfxPanel.vue:12) 明确写有“波形试听后续再接”；
     [AudioLibraryPanel.vue](src/components/library/AudioLibraryPanel.vue:154) 仅提供
     `trimStart/trimEnd` 数值输入。资源库组件中也没有音频播放或波形实现。
   - 完成条件：三类资源均可播放；`SFX/BGM` 以可视化方式调整并保存裁剪范围，
     刷新或重新导入后结果仍可恢复。

2. 恢复全局工具栏的 `TXT`（纯文本文件）导入能力，并将工程传输和导出能力放回所有主标签页都可用的全局位置。
   - 基线要求“导出完整工程、导入完整工程、导入 `TXT`、导出 `SRT`（字幕文件）、导出音频、
     生成 `MP4`（视频文件）”始终位于顶部全局工具栏。
   - [ProjectTransferPanel.vue](src/components/ProjectTransferPanel.vue:137) 只接受 JSON（JavaScript 对象表示法）；
     新工程中没有 `TXT` 导入实现。
   - [ScriptPanel.vue](src/components/ScriptPanel.vue:169) 仅在“脚本制作”页装配工程导入导出，
     `SRT/WAV/MP4` 也只在该页的工作流面板内。
   - 完成条件：切换任意主标签页仍可调用全部全局工具；导入文本后按旧页语义写入当前脚本。

3. 为新 Vue（渐进式 JavaScript 框架）工程完成并记录 P1 固定样例的端到端人工回归。
   - 现有 [P1-最小回归验收.md](../P1-最小回归验收.md:29) 和
     [P0-手动回归入口.md](P0-手动回归入口.md:22) 都要求打开仓库根目录的旧
     `index.html`，不能作为新工程通过验收的记录。
   - 新工程没有对应的稳定回归选择器；P2 验收尚缺“导入样例 -> 刷新恢复 ->
     导出 `SRT/WAV/MP4`”的实际执行记录。
   - 完成条件：在 Vite（前端构建工具）新入口运行固定样例，确认资源、台词音频和背景图在刷新后恢复，
     并成功完成 `SRT`、`WAV`（波形音频文件）、`MP4` 导出；将步骤和结果写入新工程回归记录。

## 完成后的收尾

以上三项完成并通过新工程的 P1 回归后，再复核 `重构顺序计划.md` 的 P2 验收项；
届时才删除 `P0-*.md`。
