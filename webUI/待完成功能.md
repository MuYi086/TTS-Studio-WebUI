# Vue 工作台待完成与待验收事项

本文区分“代码已接入、但未完成端到端人工回归”与“尚未实现”；静态代码存在不等于功能已验收。

## 已接入，仍需人工回归

- 顶部工程工具已在所有主标签页可见，支持完整工程 JSON 导入导出和 `TXT`（纯文本文件）导入；实现位于 [`ProjectTransferPanel.vue`](src/components/ProjectTransferPanel.vue) 与 [`AppShell.vue`](src/app/AppShell.vue)。
- 脚本工作台已接入单行/批量生成、顺序播放、`SRT` / `WAV` / `MP4` 导出；编排位于 [`useScriptAudioRuntime.ts`](src/composables/useScriptAudioRuntime.ts)。
- 固定工程可由 [`scripts/validate-p1-regression-fixture.mjs`](../scripts/validate-p1-regression-fixture.mjs) 校验结构，但该检查不替代浏览器回归。

人工回归步骤见 [`docs/本地开发与回归.md`](../docs/本地开发与回归.md)。完成前不能把 P2（第二阶段）写成已完整验收。

## 尚未完成

### 资源库试听与可视化波形裁剪

音色、SFX（场景音效）和 BGM（背景音乐）资源库可以保存资产与数值裁剪范围，但资源库组件尚未提供真实播放、进度和可视化波形裁剪：

- [`AudioLibraryPanel.vue`](src/components/library/AudioLibraryPanel.vue) 仅提供 `trimStart` / `trimEnd` 数值输入。
- [`TimbreLibraryPanel.vue`](src/components/library/TimbreLibraryPanel.vue) 的波形是静态视觉元素，不是音频波形。

完成条件：三类资源均可试听；SFX/BGM 可可视化调整并保存裁剪范围；刷新和工程导入后仍能恢复。

### 记录 Vue 工作台的 P1 端到端回归

旧 [`P1-最小回归验收.md`](../P1-最小回归验收.md) 与 `P0-*.md` 是历史对照，不能替代 Vue 入口的验收记录。

完成条件：在 `webUI/` Vite 入口运行固定样例，完成导入、刷新恢复、台词播放、`SRT` / `WAV` 导出，以及满足浏览器条件时的 `MP4` 导出，并记录环境和结果。
