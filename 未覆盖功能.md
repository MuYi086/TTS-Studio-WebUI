# 设计图中尚未覆盖的扩展功能

## 判定口径

本文只记录 `Design/高清/` 中已经画出、但旧版 [`index.html`](index.html) 没有可复用行为基线，且当前 [`webUI/`](webUI/) 也未实现完整业务闭环的功能。它们不能写成当前 WebUI 已具备的能力；后续实现前需要先明确数据结构、接口来源和兼容策略。

## 模型配置

| 设计图能力 | 当前状态 | 实现前置条件 |
| --- | --- | --- |
| TTS 连接测试、延迟仪表和服务版本展示 | 未实现 | 后端健康检查与版本接口契约 |
| TTS 协议、主机、端口和推理路径拆分配置 | 未实现 | 决定是否在兼容现有 `Base URL` 的前提下增加高级字段 |
| 配置预设、预设环境切换 | 未实现 | 预设 schema（工程结构）与敏感字段保存规则 |
| 单独导入 / 导出模型配置 | 未实现 | API Key（接口密钥）的脱敏、加密或排除策略；当前只有完整工程导入导出 |
| 配置页内测试合成与试听 | 未实现 | 测试文本、参考音频和两类 TTS 协议的统一测试流程 |

## 音色资源库

| 设计图能力 | 当前状态 | 实现前置条件 |
| --- | --- | --- |
| 音色人物封面 / 头像资源 | 未实现 | 新的图片资产字段及工程导入导出兼容规则 |
| VoxCPM2、Qwen3-TTS 等模型兼容性标签 | 未实现 | 音色与 TTS Provider（服务提供方）的能力矩阵 |
| 角色描述到音色的匹配分数与推荐卡 | 未实现 | 可解释的匹配算法或后端推荐接口 |
| 在音色资源页直接执行 AI 音色设计 | 未实现 | 复用脚本角色音色生成流程时的角色归属和命名规则 |

## 音效、BGM 与滤波器

| 设计图能力 | 当前状态 | 实现前置条件 |
| --- | --- | --- |
| 音效搜索、分类筛选和“更多”资源浏览 | 未实现 | 分类字段、索引和筛选交互规范 |
| BGM 封面图与场景 / 情绪标签 | 未实现 | BGM 图片资产和标签 schema |
| 多轨 DAW（数字音频工作站）式时间线、声像、独奏与静音 | 未实现 | 多轨工程模型；不能改变现有顺序脚本时间轴语义 |
| 多段均衡器、频谱分析、混响与空间参数 | 未实现 | Web Audio 参数模型、滤波器 schema 升级与旧工程兼容层 |
| AI 场景匹配推荐 | 未实现 | 推荐接口或本地匹配规则 |
| 调用 `8311` MOSS-SoundEffect 自动生成音效 | 未实现 | 前端 Provider、配置入口、任务状态和生成资产落库流程 |

## 脚本制作

| 设计图能力 | 当前状态 | 实现前置条件 |
| --- | --- | --- |
| 网格视图与缩放控制 | 未实现 | 台词卡密度与缩放状态保存规则 |
| 场景横幅、场景时间码与缩略图 | 未实现 | 场景实体或从 `bgImage` 块派生场景的规则 |
| 上一条 / 下一条、前后 10 秒等完整播放控制 | 未实现 | 与现有单行试听、顺序播放状态机合并 |
| 总音量、TTS、BGM、SFX 四路全局混音器 | 未实现 | 全局混音参数的持久化字段及导出语义 |
| 右侧“当前选中”专用精修面板 | 未实现 | 与现有行内编辑避免重复写入和状态分叉 |

## Prompt 管理

| 设计图能力 | 当前状态 | 实现前置条件 |
| --- | --- | --- |
| 新建任意 Prompt 模板与拖拽排序 | 未实现 | 从固定三模板升级为可扩展模板集合的 schema |
| 变量管理器 | 未实现 | 变量声明、校验和模板作用域 |
| 结构化分析结果与 JSON 预览 | 未实现 | 测试输入、解析器和错误态规范 |
| Guardrails（约束规则）可视化管理 | 未实现 | 约束规则数据模型及与 Prompt 文本的合并方式 |
| Prompt 版本历史、回滚和来源切换 | 未实现 | 版本存储、容量上限和迁移策略 |
| “测试分析”沙盒 | 未实现 | 测试请求是否写回工程、计费和取消语义 |

## 不作为缺口的视觉元素

设计图中的示例人物、场景图、BGM 封面、波形和推荐分数属于演示数据。当前仓库没有对应授权素材和真实业务数据，因此不应把这些示例直接写入产品或伪造成真实结果。
