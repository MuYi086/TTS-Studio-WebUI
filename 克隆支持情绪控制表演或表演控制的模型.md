# 克隆支持情绪控制表演或表演控制的模型

> 调研日期：2026-07-28。范围是 LongCat-AudioDiT README 的 **Experimental Results on Seed Benchmark** 表格，以及任务额外指定的 `dots.tts`、`IndexTTS2`、`OmniVoice`、`VoxCPM2`。这里的“支持”必须有官方 README、API 或示例证明；不把“听起来有情绪”或“可单独设计一条新音色”误记为“克隆时可控”。

## 口径

- **开源模型**：官方公开可访问的代码和/或可推理权重；只有论文、演示页、商业 API，或只有同名但未公开的内部变体，不加入 `a`。
- **直接控制**：同一次克隆调用中，既传入参考音频（保留目标音色），又能传入情绪、风格或表演指令/参数。
- **提示音频迁移**：没有独立控制参数，但官方明确说明可从克隆提示音频迁移情绪或表演风格。单列为次级能力，不能当作独立情绪旋钮。
- **两阶段控制**：先通过官方音色设计接口生成带表演要求的参考音频，再将该音频作为克隆提示复用；它可用于角色表演，但不是单次克隆接口。

LongCat 表格来源：[LongCat-AudioDiT README：Experimental Results on Seed Benchmark](https://github.com/meituan-longcat/LongCat-AudioDiT#experimental-results-on-seed-benchmark)。

## 数组 `a`

以下 JavaScript 数组保留了任务要求的 `push` 操作；`IndexTTS2` 原本已在基准表的开源集合中，因此最终检索时以 `Set` 去重。

```js
const a = [
  'MaskGCT',
  'E2-TTS',
  'F5-TTS',
  'ZipVoice',
  'SparkTTS',
  'FireRedTTS',
  'Qwen2.5-Omni',
  'Qwen2.5-Omni_RL',
  'CosyVoice',
  'CosyVoice2',
  'FireRedTTS-1S',
  'IndexTTS2',
  'VoxCPM',
  'MOSS-TTS',
  'Qwen3-TTS',
  'LongCat-AudioDiT-1B',
  'LongCat-AudioDiT-3.5B',
];

a.push('dots.tts', 'IndexTTS2', 'OmniVoice', 'VoxCPM2');

const uniqueModels = [...new Set(a)];
// 20 个唯一模型；a 本身有 21 项，因为 IndexTTS2 被按要求再次加入。
```

### 未加入 `a` 的表格条目

| 条目 | 未纳入原因 |
| --- | --- |
| `GT` | 评测真值，不是 TTS 模型。 |
| `Seed-DiT`、`Seed-ICL` | 闭源基线；当前无官方公开推理仓库。 |
| `F5R-TTS` | 找到论文和演示页，但未找到官方公开代码/权重仓库，不能按开源模型处理。 |
| `DiTAR` | 当前公开的对比资料将其标为未开源；未找到官方可用推理仓库。 |
| `MiniMax-Speech` | 当前模型对比表明确标为非开源；公开的是商业服务/技术报告，不是可本地推理的官方开源模型。 |
| `CosyVoice3-1.5B` | 官方仓库当前公开的是 `Fun-CosyVoice3-0.5B`；未找到该 1.5B 变体的官方公开权重和克隆说明。 |
| `CosyVoice3.5` | 有产品发布和 FreeStyle 指令控制信息，但官方仓库仍停留在 CosyVoice 3，未找到公开代码与权重。 |

`Qwen2.5-Omni_RL` 保留在 `a` 中，是因为官方 Qwen2.5-Omni README 明确报告了 `7B_RL` 的 Seed-TTS 指标；不过它没有单独命名的下载项或单独克隆 API，以下与 `Qwen2.5-Omni` 共用同一官方仓库进行核验。

## 逐模型官方来源与克隆接口核验

“克隆示例”一列链接到官方文档中的命令、API 或示例文件；“结论”只针对**克隆时**的情绪/表演控制，不评价音质。

| 模型 | 官方 GitHub / Hugging Face | 官方克隆示例 | 结论 |
| --- | --- | --- | --- |
| MaskGCT | [Amphion / MaskGCT](https://github.com/open-mmlab/Amphion/tree/main/models/tts/maskgct) | [项目演示：Speech Style Imitation](https://maskgct.github.io/#speech-style-imitation) | **提示音频迁移**。官方明确写明可从 prompt speech 学习 emotion、accent 等说话风格。 |
| E2-TTS | [SWivid / F5-TTS](https://github.com/SWivid/F5-TTS)（官方仓库同时提供 E2-TTS 复现） | [CLI `--ref_audio` / `--ref_text`](https://github.com/SWivid/F5-TTS#2-cli-inference) | 仅参考音频克隆；文档没有独立情绪/表演控制参数。 |
| F5-TTS | [SWivid / F5-TTS](https://github.com/SWivid/F5-TTS) | [CLI `--ref_audio` / `--ref_text`](https://github.com/SWivid/F5-TTS#2-cli-inference) | 仅参考音频克隆；没有官方的克隆情绪控制接口。 |
| ZipVoice | [k2-fsa / ZipVoice](https://github.com/k2-fsa/ZipVoice) | [`--prompt-wav` / `--prompt-text`](https://github.com/k2-fsa/ZipVoice#11-inference-of-a-single-sentence) | 仅提示音频克隆；未见情绪或表演控制参数。 |
| SparkTTS | [SparkAudio / Spark-TTS](https://github.com/SparkAudio/Spark-TTS) | [`--prompt_text` / `--prompt_speech_path`](https://github.com/SparkAudio/Spark-TTS#basic-usage) | 有克隆，也可在**独立虚拟音色生成**中调性别、音高、语速；官方示例未将这些控制与参考音频克隆组合。 |
| FireRedTTS | [FireRedTeam / FireRedTTS](https://github.com/FireRedTeam/FireRedTTS) | [`synthesize(prompt_wav, prompt_text, text, ...)`](https://github.com/FireRedTeam/FireRedTTS#basic-usage) | 仅提示音频克隆；未见独立情绪/表演控制参数。 |
| Qwen2.5-Omni | [QwenLM / Qwen2.5-Omni](https://github.com/QwenLM/Qwen2.5-Omni) | [输出音色仅 `Chelsie` / `Ethan`](https://github.com/QwenLM/Qwen2.5-Omni#change-voice-type-of-output-audio) | 官方 README 提供的是预设输出音色切换，未提供参考音频克隆示例，因此不符合本表目标。 |
| Qwen2.5-Omni_RL | [QwenLM / Qwen2.5-Omni](https://github.com/QwenLM/Qwen2.5-Omni) | [README 的 `Qwen2.5-Omni-7B_RL` 评测行](https://github.com/QwenLM/Qwen2.5-Omni#performance) | 与上行共用公开源码；未找到独立的官方克隆接口或情绪控制示例。 |
| CosyVoice | [FunAudioLLM / CosyVoice](https://github.com/FunAudioLLM/CosyVoice) | [`inference_zero_shot(..., prompt_text, prompt_wav)`](https://github.com/FunAudioLLM/CosyVoice/blob/main/example.py#L28-L30) | 有零样本克隆，但 v1 的 `inference_instruct` 示例使用预置说话人，不与 prompt 音频组合；不计为直接可控克隆。 |
| CosyVoice2 | [FunAudioLLM / CosyVoice](https://github.com/FunAudioLLM/CosyVoice) | [`inference_instruct2(tts_text, instruct, prompt_wav)`](https://github.com/FunAudioLLM/CosyVoice/blob/main/example.py#L66-L67) | **直接控制**。同一调用既有参考音频，也有自然语言指令；项目 README 明确将 emotion 列为指令支持项。 |
| FireRedTTS-1S | [FireRedTeam / FireRedTTS（`FireRedtts-1s` 分支）](https://github.com/FireRedTeam/FireRedTTS/tree/FireRedtts-1s) | [`synthesize(prompt_wav, prompt_text, ...)`](https://github.com/FireRedTeam/FireRedTTS/tree/FireRedtts-1s#basic-usage) | 仅提示音频克隆；未见官方情绪/表演控制参数。 |
| IndexTTS2 | [index-tts / index-tts](https://github.com/index-tts/index-tts) | [`spk_audio_prompt` 搭配 `emo_audio_prompt`、`emo_vector` 或 `emo_text`](https://github.com/index-tts/index-tts#-using-indextts2-in-python) | **直接控制**。音色提示与情绪提示解耦，可传第二段情绪音频、8 维情绪向量或文字情绪描述。 |
| VoxCPM | [OpenBMB / VoxCPM](https://github.com/OpenBMB/VoxCPM) | [VoxCPM 1.x 的 reference/prompt 克隆能力说明](https://github.com/OpenBMB/VoxCPM#models--versions) | 旧版聚焦高保真/续接式克隆；当前官方文档把显式“可控克隆”作为 VoxCPM2 能力，故旧版不列为合格。 |
| MOSS-TTS | [OpenMOSS / MOSS-TTS](https://github.com/OpenMOSS/MOSS-TTS) | [`reference=[ref_audio]` 的 Voice cloning 示例](https://github.com/OpenMOSS/MOSS-TTS#quickstart) | 支持克隆和时长/停顿控制，但没有官方情绪或表演指令与克隆同时使用的示例；不将停顿标记等同于表演控制。 |
| Qwen3-TTS | [QwenLM / Qwen3-TTS](https://github.com/QwenLM/Qwen3-TTS) | [Voice Design then Clone 工作流](https://github.com/QwenLM/Qwen3-TTS#voice-design-then-clone) | **两阶段控制**。Base 克隆接口自身只收参考音频/转写；官方示例先用 VoiceDesign 的 `instruct` 造出带角色/表演的参考音频，再制作可复用 clone prompt。 |
| LongCat-AudioDiT-1B | [meituan-longcat / LongCat-AudioDiT](https://github.com/meituan-longcat/LongCat-AudioDiT) | [CLI `--prompt_text` / `--prompt_audio`](https://github.com/meituan-longcat/LongCat-AudioDiT#cli-inference) | 仅提示音频克隆；`guidance_method` 是采样引导而非情绪/表演控制。 |
| LongCat-AudioDiT-3.5B | [meituan-longcat / LongCat-AudioDiT](https://github.com/meituan-longcat/LongCat-AudioDiT) | [同一 CLI / Python 克隆示例](https://github.com/meituan-longcat/LongCat-AudioDiT#voice-cloning-with-prompt-audio) | 同 1B：无官方独立情绪/表演控制接口。 |
| dots.tts | [studio-dots-ai / dots.tts](https://github.com/studio-dots-ai/dots.tts) | [`--prompt-audio` / `--prompt-text`](https://github.com/studio-dots-ai/dots.tts#cli) | README 声称有 emotional expressiveness（情绪表现力），但克隆接口只公开了参考音频、转写、采样步数等参数；不计为可控情绪。 |
| OmniVoice | [k2-fsa / OmniVoice](https://github.com/k2-fsa/OmniVoice) | [Voice Cloning：`ref_audio` + `ref_text`](https://github.com/k2-fsa/OmniVoice#voice-cloning) | Voice Design 的 `instruct` 与 Voice Cloning 是分开的三种生成模式；克隆调用未公开情绪/表演指令，故不计入。 |
| VoxCPM2 | [OpenBMB / VoxCPM](https://github.com/OpenBMB/VoxCPM) / [权重](https://huggingface.co/openbmb/VoxCPM2) | [`reference_wav_path` + 文本中的 style control](https://github.com/OpenBMB/VoxCPM#-controllable-voice-cloning) | **直接控制**。同一 `generate` 调用以参考音频保留音色，并用如 `(slightly faster, cheerful tone)` 的控制描述调节速度、情绪、风格。 |

## 符合条件的模型

### 一、直接在克隆调用中控制

| 模型 | 最小接口形态 | 可控维度 | 使用要点 |
| --- | --- | --- | --- |
| **IndexTTS2** | `spk_audio_prompt` + `emo_audio_prompt` / `emo_vector` / `emo_text` | 情绪参考、8 维情绪强度、文字情绪描述、情绪混合权重 `emo_alpha` | 音色和情绪分离得最明确；适合“同一个角色、多种表演”。 |
| **CosyVoice2** | `inference_instruct2(tts_text, instruct, prompt_wav)` | 自然语言指令；官方列出情绪、语速、音量、语言/方言等 | 输入中仍包含 prompt 音频，故可保留音色后改变表达。实际项目应测试不同指令与参考音频组合的稳定性。 |
| **VoxCPM2** | `generate(text='(控制描述)台词', reference_wav_path=...)` | 情绪、语速、整体风格/表演；也有 Ultimate Cloning 保留细微韵律 | 官方给出“欢乐语气、稍快”的克隆示例，接口证据最直接。 |

### 二、可用于表演，但不是独立情绪参数

| 模型 | 方式 | 边界 |
| --- | --- | --- |
| **MaskGCT** | 用带目标情绪/口音/风格的 prompt speech 做零样本提示；官方演示明确称其可学习 emotion 与 style。 | 情绪随参考音频整体迁移，不能在保留同一声线时单独给一个情绪向量或文本指令。 |
| **Qwen3-TTS** | `VoiceDesign(instruct)` 先生成角色参考音频，再 `create_voice_clone_prompt` / `generate_voice_clone` 复用该角色。 | 是两个模型/两步工作流；Base 的 `generate_voice_clone` 本身没有 `instruct` 参数。 |

## 结论与选型

- 需要“**同一个克隆角色 + 可重复切换喜怒哀乐**”时，优先用 **IndexTTS2**；其官方接口同时提供情绪音频、向量和文本三种条件。
- 需要“**用一句自然语言描述来导演已克隆角色**”时，优先用 **VoxCPM2**；官方示例直接把控制描述与 `reference_wav_path` 放在同一次调用中。
- 需要“**克隆音色 + 方言/语速/情绪等指令**”且接受模型族接口时，选择 **CosyVoice2** 的 `inference_instruct2`。
- 需要从现成演员台词迁移整段演法、且不要求分离控制时，可用 **MaskGCT**；需要先定义角色再长期复用时，可采用 **Qwen3-TTS** 的“设计后克隆”工作流。

## 证据复核要点

1. LongCat 基准表本身只报告 WER/CER 与说话人相似度，不代表模型一定具备情绪控制；因此本调研以各模型官方 API/README 为最终判据。
2. “情绪表现力”不等于“情绪可控”。例如 dots.tts 只公开了 prompt 克隆参数；没有单独的情绪条件，因此没有进入直接控制名单。
3. “音色设计”不等于“克隆控制”。OmniVoice 和 SparkTTS 都提供独立的音色/属性生成，但官方示例没有把该指令和参考音频克隆合并，故未纳入。
4. 外部参考音频可能涉及肖像、表演及声音人格权。所有克隆应只使用已获得权利人明确授权的音频，并按适用法律和平台规则标识合成内容。
