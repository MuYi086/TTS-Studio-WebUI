# 任务计划 2：三种候选模型的上下文情感合成与 AI 原始输出

## 直接结论

可以把上下文情绪、表演意图和参考音色选择写入 AI 原始输出并供人工调试，但在当前代码和当前三个后端接口下，它们不会作为“每条台词的情绪参数”传入 Qwen3-TTS Base、OmniVoice 或 dots.tts-base。三者当前可靠的共同路径是：正确的目标文本、成对的参考音频与逐字参考转写，以及参考片段本身携带的音色/韵律。

因此，下一版不能宣称“已支持情绪参数控制”。应先实现模型能力声明、可审阅的表演计划和参考音色档案路由；只有上游模型与本地 adapter（适配器）实际验证可接受某个参数时，才在 UI 中向用户开放它。

本文件仅为设计方案，不修改 `index.html`、`project-storage.js`、后端接口或现有 `webUI/ai_tts.json`。

## 当前链路与缺口

当前 `webUI/ai_tts.json` 已保存 `emotion`、`intensity`、`break_duration`，但它是旧调试样例，且字段没有自动映射为三种候选模型的表演控制。根目录页面中，`generateLineAudio` 对“参考文本克隆”发送的关键内容是：

```json
{
  "audio_path": "角色参考音频路径",
  "text": "当前台词正文",
  "prompt_text": "参考音频中真实说出的逐字文本"
}
```

`prompt_text` 的职责是参考转写，不能改造成“用害怕语气读”之类指令。`line.speed` 是浏览器播放/导出层的播放速率，不等价于已向所有 TTS 模型发送的合成语速参数。现有系统情绪预设的向量链路是 IndexTTS2 的历史能力，不能假定另外三种克隆模型会消费。

## 模型能力矩阵（以当前接入为准）

| 模型 | 克隆时的情绪/自然语言指令参数 | 当前可验证的辅助控制 | 处理决定 |
| --- | --- | --- | --- |
| Qwen3-TTS-12Hz-1.7B-Base | 不支持。官方 Base 克隆接口为参考音频、参考转写、目标文本与语言；本地 worker 仅额外读取采样 `top_p`、`temperature`。 | 采样参数影响随机性，不是可预测的情绪旋钮。 | 不向克隆请求发送 `emotion`/`instruction`；用参考档案、自然标点和人工试听控制。 |
| OmniVoice | 当前克隆路径不支持。官方 `instruct` 属于无参考音频的 Voice Design 模式；本地 clone worker 也没有转发它。 | 后端可接受 `speed` 或 `duration`，但前端尚未接入；二者只控制时长/语速，不表达情绪。 | 克隆模式先只用参考档案。若要研究 `instruct + ref_audio`，先以官方可复现代码证明二者可共存，不能直接上生产。 |
| dots.tts-base | 不支持。官方推荐 `prompt_audio + prompt_text` 的续接式克隆；当前 worker 转发的是采样步数、CFG 等参数。 | `guidance_scale`、`num_steps` 等是质量/采样参数；不能把较高 CFG 当成“更激动”的通用语义控制。 | 不发送情绪指令；锁定采样配置后，通过参考档案和文本分段做 A/B。 |

参考： [Qwen3-TTS 官方克隆示例](https://github.com/QwenLM/Qwen3-TTS/blob/main/README.md)、[OmniVoice 官方 Voice Cloning/Voice Design 文档](https://github.com/k2-fsa/OmniVoice)、[dots.tts-base 官方模型卡](https://huggingface.co/dots-studio/dots.tts-base)。本地实现在 `~/github/TTS-and-VoiceDesign/api/qwen3_tts_worker.py`、`omnivoice_tts_worker.py` 与 `dots_tts_worker.py`。

## 建议的调试文件格式

不要直接覆写旧 `webUI/ai_tts.json`；保留它作为历史样例，新调试导出使用新文件名（如 `ai_tts_debug_v2.json`）并显式标记版本。它是可复现计划，不含 API Key、绝对本机路径或音频二进制。

```json
{
  "kind": "unitale-ai-tts-debug",
  "schemaVersion": 2,
  "createdAt": "2026-07-28T00:00:00.000Z",
  "source": {
    "scriptId": "...",
    "rawScriptHash": "sha256:...",
    "analysisPromptVersion": "production-candidates-v1"
  },
  "lines": [
    {
      "lineId": "line_...",
      "type": "dialogue",
      "role": "老李",
      "text": "别接那个电话！",
      "emotion": "害怕",
      "intensity": "较强",
      "breakDuration": 0.35,
      "performance": {
        "emotionStrength": 4,
        "delivery": ["压低", "急促"],
        "pace": "快",
        "referenceProfile": "tense",
        "continuity": "确认死亡事实后立刻阻止对方接听"
      },
      "synthesisPlan": {
        "requestedModel": "Qwen3-TTS-12Hz-1.7B-Base",
        "capabilityMode": "reference-profile-only",
        "voiceProfileId": "laoli-tense-v1",
        "referenceTranscriptSha256": "sha256:...",
        "targetTextSha256": "sha256:...",
        "requestFields": ["audio_path", "text", "prompt_text"],
        "unsupportedControls": ["emotion", "instruction"]
      }
    }
  ]
}
```

设计原则：

- `text` 必须与最终请求的目标文本完全相同；任何标签都只能在 `performance` 中。
- 只记录参考转写哈希和档案 ID，默认不导出参考音频路径与原文，防止调试文件泄露授权素材位置。
- `requestFields` 应由 adapter 最终实际发出的字段生成，不能由 LLM 猜测。
- 导出前要求确认；用户可选择是否包含 `continuity` 等可能含原著片段的调试信息。
- 成功后追加 `result`（音频资产键、输出哈希、时长、模型 revision、采样参数、耗时）；失败后追加无敏感信息的错误类别。这样才能复现“哪一条、以哪套参考、为何听起来不同”。

## 推荐的数据与路由结构

### 1. 表演计划与模型能力分离

LLM 只产出模型无关的 `performance`，而 adapter 持有白名单能力表。建议的语义为：

```text
modelCapability = {
  cloneReferenceTranscript: required,
  performanceControl: "reference-profile-only",
  acceptsEmotionParameter: false,
  acceptsInstructionInCloneMode: false,
  acceptsSpeedInCloneMode: false | true,
  acceptsDurationInCloneMode: false | true
}
```

这里的 `true` 也不表示前端立即开放：必须有端到端探针、模型 revision 固定、回归样本和人工结果后才升级能力表。未知字段一律不发送，避免“后端忽略字段但 UI 显示成功”的假控制。

### 2. 参考音色档案

每个角色的一个音色可拥有多个已授权参考档案：`neutral`、`restrained`、`tense`、`emphatic`。每个档案至少保存稳定 ID、音频 asset key、真实参考转写、时长、授权记录、适用模型和人工验收状态；每条台词保存选择的档案 ID，而不是复制文件。

开始只启用 `neutral` 与 `tense`。LLM 的 `referenceProfile` 只是建议，用户可在台词上改成另一个档案或锁定角色全局默认。不存在、未通过验收或不适用于当前模型的档案必须回退 `neutral` 并给出可见提示。

### 3. 合成执行顺序

```text
小说/剧本
  → LLM 分析（正文 + performance）
  → 人工审核、锁定文本与档案建议
  → 按当前模型能力表选择参考档案
  → 只发送该模型允许的字段
  → 保存音频、实际请求快照与结果哈希
  → 试听/回退/人工重选档案
```

任何“上下文”都应在前两步辅助选择档案和拆分边界；当前三个模型不会从邻近台词自动获得跨请求记忆。不得为了传上下文把上一句或角色说明拼接到目标文本，否则 CER 和字幕都会偏离原文。

## 分阶段实验设计

### 阶段 A：零风险观察

使用同一批 `ai_deal.json`、同一参考音频/转写和冻结采样参数，导出旧 Prompt 与新 Prompt 的计划。检查：原文拼接完全一致、行长符合约束、所有枚举合法、没有标签进入 `text`、所有旧工程仍可导入。

### 阶段 B：档案收益验证

选 20–30 条覆盖平静、压抑、惊恐、愤怒、叙述转折的短句。每个模型以 `neutral` 和 `tense` 两个同角色档案生成，随机化文件名，至少由两名试听者独立评分：文本可懂度、角色一致性、目标情绪贴合、自然度、是否过演、是否有异常音。不要让评分者知道模型和档案。

### 阶段 C：生产候选决策

对阶段 B 通过的配置，用冻结输入重跑：

1. SenseVoice CER 与 Whisper CER：原始值和独立名次分别报告，禁止平均；
2. 适用的 Seed-TTS 流程：按其原始指标报告，不能塞进 V6–V9 的受限长音频报告；
3. 人工盲听：至少记录每条胜负、弃权、异常类型和听音设备/环境；
4. 生产指标：首包/总耗时、失败率、重试率、显存冲突、单位分钟成本和人工修复时间。

淘汰阈值、评分权重和“并列如何处理”必须在生成结果前由产品负责人冻结。没有这些规则时，只能输出对比表，不能宣称自动选出唯一生产模型。

## 实现后的兼容与验证清单

- 不改 `dialogue`、`bgm`、`bgImage` 类型，不删除 `emotion`、`intensity`、`break_duration`、`sfx` 或原有资产键。
- 新字段必须可缺省；导入旧工程时不强制重生成音频或参考转写。
- 调试导出不等于工程导出，不能携带 `localStorage` 中的 API Key。
- 实现页内逻辑后，运行仓库规定的内联脚本语法检查；涉及 schema/资产时再运行 P1 fixture（固定回归样例）验证。
- 为每个 adapter 添加请求体单测/捕获测试：Qwen 与 dots 不含 `emotion`/`instruction`；Omni clone 同样不含 `instruct`，除非一份独立、已验证的能力升级设计获批准。
